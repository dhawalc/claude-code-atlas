export function Footer() {
  return (
    <footer className="site-footer">
      <div>Built to make the codebase legible in public.</div>
      <div>
        Source snapshot lives in the app repo and can be refreshed from the
        analysis repo.
      </div>
    </footer>
  );
}
